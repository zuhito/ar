{
  "name": "ar-editor",
  "private": true,
  "description": "XML-driven A-Frame AR editor with live preview",
  "scripts": {
    "fetch-assets": "node scripts/fetch-assets.mjs",
    "pretest": "node scripts/fetch-assets.mjs",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.1",
    "pngjs": "^7.0.0"
  }
}
